import styled from '@emotion/styled'

const Container = styled.div`
  ${({ theme }) => theme.screen}
  font-size: 18px;
  letter-spacing: 0;
  min-height: 115px;
  flex: 1;
`

export default Container
